# Oracle SQL 실행계획 성능진단 Agent - 의존성
# 설치: pip install -r requirements.txt
# (sds-ax-practice 과정 공유 venv를 쓰는 경우 이미 대부분 설치되어 있음)

python-dotenv
boto3

# LangChain / LangGraph
langchain
langchain-aws
langchain-classic
langchain-community
langchain-text-splitters
langchain-chroma
langgraph
langgraph-checkpoint-sqlite
langgraph-supervisor
langchain-mcp-adapters

# RAG
rank-bm25
kiwipiepy

# MCP (Oracle SQLcl 연동 확장 경로)
mcp
fastmcp

# 주의: Oracle 대상 DB 접근은 SQLcl MCP 단일 경로만 사용한다.
# 직접 DB 드라이버(oracledb, cx_Oracle)는 의존성으로 선언하지 않는다.

# API 서버
fastapi
uvicorn
httpx

# Observability (Langfuse, self-hosted via docker-compose)
langfuse

# 평가 (RAGAS + Bedrock 어댑터)
ragas
anthropic[bedrock]

rich
pytest
